const address = {
    city: "Bangalore"
};

// Case 1
const { city } = address;
console.log(city);

// Case 2
const { city: town } = address;
console.log(town);

// What is the difference between Case 1 & 2.
