const jay = {
    name: 'jay'
};

const studentObj = {};
studentObj[jay] = 500;
console.log(studentObj);

const studentMap = new Map();
studentMap.set(jay, 500);
console.log(studentMap);

// What is the Output?